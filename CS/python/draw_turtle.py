from turtle import * # Loads commands used to draw

color('red') # Set the color of the pen
forward(200) # Move the turtle forward by 200 units
left(90) # Turn the turtle left by 90 degrees
# turtle will now be facing up

forward(200)
left(90)
forward(200)
left(90)
forward(200)

done() # Finish drawing